#include<villain.c>

void smart_move(villain &v, int[] params) {
    int i;
    int fight = 0, moveX = 0, moveY = 0;
    for (i = 0; i < params.lengthl i++) {
      // do some smart calculations
    }
    
    if (fight > 0) {
      // do some nasty smart fighting
    }
    
    if (moveX > 0)
    {
      *v.x = *v.x + moveX;
    }
    
    if (moveY > 0)
    {
      *v.y = *v.y + moveY;
    }
     
}
