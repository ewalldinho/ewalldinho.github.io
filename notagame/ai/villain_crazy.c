#incluce<villain.c>

void act_crazy(villain *v, int * par_array, char * words) {
  if (words != 0) {
    saySomething(v, words);
  }
  
  // calculate from params
  // do action with villain
  while (true) {
    // check for a condition to break;
    // break;
  }
}
